databases:
  - name: joai-db
    databaseName: joai_db
    user: joai_user
    password: xYl0e8Tlmz7ElkXu7w2H7m0jzIAducm8
    port: 5432
    host: dpg-d47pj5chg0os73frtvsg-a
    plan: free # Free tier: 1 GB storage, expires after 90 days
    ipAllowList: [] # Allow connections from any IP

services:
  - type: web
    name: joai
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DB_CONNECTION
        value: postgresql
      - key: DATABASE_URL
        fromDatabase:
          name: joai-db
          property: connectionString
  - type: pserv
    name: joai-db
    runtime: postgresql
    ipAllowList: []  # Only accessible by other services in this project